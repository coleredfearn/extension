// import pizza as '/pizza.png';
// import pizza from '.file!/pizza.png';

// console.log(pizza);
const img = document.createElement('img'); //<img src = 'pizza.png'> 
img.src = './pizza.png'; 

// Array.from(element.attributes).forEach(attr => {
//   if (`${attr.valueinput}`);
//  })

//get the input from the form in the popup.html
const input = document.getElementById("myInput").value; //error: cannot get property value of null
console.log(input)

// we need to iterate through the alt atribute to see if the input is included
for (let i = 0; i < elem.attributes.length; i++) {
  let attrib = elem.attributes[img];
  if (attrib.matches(`${input}`)
}
// if alt attribute includes input, replace img with our pizza image 
$("[testdata]").each(function(){  }

//save the image to block as a variable
let imageToBlock = $('img[alt="`${input}`"]');

//set imageToBlock as a parent to save location
// let parentImage = imageToBlock.parent();

//replace the current image with the new pizza image
document.body.replaceChild(pizza, imageToBlock);
//parent                new child   old child

//parentNode.replaceChild(newChild, oldChild);

//let imgElement = document.querySelector('img[alt="the input value"]')

//blockedImage.parentNode.removeChild(blockedImage)
//.prepend(inserts the specified content as the first child of each element in the jQuery collection)


//render

